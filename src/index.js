import ReactDOM from "react-dom";
import './index.css'; //we can't import css in javascripts but here we have already imported RectDom that means we are working  with React 
import App from "./App";
ReactDOM.render(<App/> , document.getElementById('root')); // syntax will be (what we have to add , where we have to add )  
//JSX= javascripts XML